spring.application.name=recipesorg-api

spring.datasource.url=jdbc:postgresql://db:5432/recipesorg
spring.datasource.username=admin
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=none



# JWT settings
jwt.secret=C0NtR07S3cR5Ts@4%6hSf
jwt.expiration-ms=3600000

# Reduce Hibernate/SQL logging noise: keep warnings/errors only
# Disable the logger that prints raw SQL and bind parameter logs
logging.level.org.hibernate.SQL=ERROR
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=ERROR
logging.level.org.hibernate.tool.hbm2ddl=ERROR

# Keep overall application logging at INFO (change to WARN if you prefer fewer messages)
logging.level.root=INFO